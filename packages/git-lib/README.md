# git-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build git-lib` to build the library.

## Running unit tests

Run `nx test git-lib` to execute the unit tests via [Vitest](https://vitest.dev/).
